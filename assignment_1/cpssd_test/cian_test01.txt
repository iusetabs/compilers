constant f:integer := 123;
variable e:boolean;

vOid meme(test:void, me:integer, you:boolean) is
  variable /*Oops stupid comment*/ k:integer;
begin
  my_id := /* Nest /*nested*/*/ your_id;
  my_id2(test_arg); // hly moly ijesoirhw38rf
  return(expression);
eND

void meme(test:void, me:integer, you:boolean) is
  variable k:integer;
begin
  my_id := your_id;
  my_id2();
  my_id2(test_arg);
  my_id2(test_arg, test_arg2);
  sKip;
  while ~~(my_id > ed) begin
   my_id := test + (3) - me(e, k) + (12 - kke); //breaks
  end
  if my_cond < test & ~ (e >= u) | (f = e) begin
    my_id := -3;
  end else begin
    my_id := 23 + 27 - (-6 - -e);
    if ((my_cond2) < k) begin //TODO failing here
    if (my_cond2 < k) begin
      my_id2 := test2;
    end else begin
      skip;
    end
  end  
end
  return(expression);
end

void meme2() is
  constant k:integer := 123;
begin
  statement_block2 := test2;
  return(expression2);
end

main
begin
  variable e:integer;
  variable d:void;
  statement_block_main := test2;
end

